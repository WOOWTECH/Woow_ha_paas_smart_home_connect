---
name: Component 骨架與常數定義
status: closed
created: 2026-03-02T14:36:04Z
updated: 2026-03-02T14:36:04Z
github: [Will be updated when synced to GitHub]
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Component 骨架與常數定義

## Description
建立 `woow_paas_smart_home` custom component 的基礎骨架檔案，包含所有常數定義、manifest、UI 文字和 service 定義。

## Acceptance Criteria
- [ ] `const.py` 包含 DOMAIN, API base URL (占位符), 所有 API endpoint paths, config keys, platform list, error constants
- [ ] `manifest.json` 包含正確的 domain, name, version, config_flow: true, dependencies, codeowners, iot_class
- [ ] `strings.json` 包含 config flow 所有步驟的 UI 文字（OAuth, workspace selection, home selection）和 error messages
- [ ] `services.yaml` 定義 start_tunnel, stop_tunnel, get_status 三個 service

## Technical Details
- `const.py` 中 API base URL 使用占位符常數，未來可由 config entry 設定
- `manifest.json` 的 `iot_class` 設為 `cloud_push`
- `strings.json` 需要涵蓋 OAuth2 authorize step 和後續的 workspace/home 選擇步驟
- `services.yaml` 中 get_status 標記 `supports_response: optional`
- platforms 列表: `["sensor", "binary_sensor"]`

## Dependencies
- 無

## Effort Estimate
- Size: S
- Parallel: true
